# config – Configuration Files

Purpose
-------
Optional runtime configuration (if present). Keep config files simple and human-readable.

Conventions
-----------
- Prefer JSON or INI for small settings.
- Avoid storing secrets in the repository.
